# Example tests with @larsthorup/testrunner

Notes to options used for `npm test`:

- `NODE_OPTIONS=--loader=esmock` is required for the `esmock` example
- `IS_TEST=1` is required for the in-source example
